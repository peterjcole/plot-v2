'use client';

export default function MapArea() {
  return (
    <div
      style={{
        flex: 1,
        background: 'var(--p0)',
        position: 'relative',
        overflow: 'hidden',
      }}
    />
  );
}
